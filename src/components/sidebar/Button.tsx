import "./Button.css";
const Button = () => {
  return (
    <button className="button">
      <span>Create New</span>
      <span className="plus">+</span>
    </button>
  );
};

export default Button;
